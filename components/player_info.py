class PlayerInfo(object):

    def __init__(self, number, is_local, host, port):
        self.snake = []
        self.number = number
        self.is_local = is_local
        self.host = host
        self.port = port

