class SnakeInfo(object):

    def __init__(self, player, is_head, is_tail, next_segment):
        self.player = player
        self.is_head = is_head
        self.is_tail = is_tail
        self.next_segment = next_segment

